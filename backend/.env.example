# Backend environment variables
#
# NOTE: Do not commit real secrets to .env. Use this file as a reference only.

# Path to the SQLite database file provided by the database container.
# The database container uses this environment variable name as its contract (db_env_vars: SQLITE_DB).
SQLITE_DB=/home/kavia/workspace/code-generation/habit-tracker-pro-330007-330023/database/myapp.db
